<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:DeepTime.LithoMind.Desktop.ViewModels.Pages"
             x:Class="DeepTime.LithoMind.Desktop.Views.MappingLayerView"
             x:DataType="vm:MappingLayerViewModel">

    <UserControl.Styles>
        <!-- 图层项样式 -->
        <Style Selector="TreeViewItem:selected /template/ Border#PART_LayoutRoot">
            <Setter Property="Background" Value="#E3F2FD"/>
        </Style>
        
        <!-- 透明度滑块样式 -->
        <Style Selector="Slider.opacity-slider">
            <Setter Property="Minimum" Value="0"/>
            <Setter Property="Maximum" Value="100"/>
            <Setter Property="Width" Value="60"/>
            <Setter Property="Height" Value="16"/>
        </Style>
    </UserControl.Styles>

    <Border Background="#F5F6FA">
        <Grid>
            <!-- 主内容 -->
            <DockPanel>
                <!-- 顶部工具栏 -->
                <Border DockPanel.Dock="Top" Background="#2C3E50" Padding="8,6">
                    <Grid ColumnDefinitions="*,Auto">
                        <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="4">
                            <TextBlock Text="🗂️" FontSize="14"/>
                            <TextBlock Text="图层管理" FontSize="13" Foreground="White" FontWeight="SemiBold"/>
                        </StackPanel>
                        <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="2">
                            <Button Content="➕" 
                                    ToolTip.Tip="添加图层"
                                    Command="{Binding ShowAddLayerCommand}"
                                    Background="Transparent"
                                    Foreground="White"
                                    BorderThickness="0"
                                    Padding="6,4"/>
                            <Button Content="👁️" 
                                    ToolTip.Tip="全部显示"
                                    Command="{Binding ShowAllLayersCommand}"
                                    Background="Transparent"
                                    Foreground="White"
                                    BorderThickness="0"
                                    Padding="6,4"/>
                            <Button Content="🙈" 
                                    ToolTip.Tip="全部隐藏"
                                    Command="{Binding HideAllLayersCommand}"
                                    Background="Transparent"
                                    Foreground="White"
                                    BorderThickness="0"
                                    Padding="6,4"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- 图层列表 -->
                <TreeView ItemsSource="{Binding Layers}"
                          SelectedItem="{Binding SelectedLayer, Mode=TwoWay}"
                          Background="Transparent">
                <TreeView.ItemTemplate>
                    <TreeDataTemplate ItemsSource="{Binding Children}">
                        <Border Padding="4,3" 
                                Background="Transparent"
                                CornerRadius="4"
                                Margin="0,1">
                            <Grid ColumnDefinitions="Auto,Auto,*,Auto,Auto,Auto">
                                <!-- 可见性复选框 -->
                                <CheckBox Grid.Column="0"
                                          IsChecked="{Binding IsVisible, Mode=TwoWay}"
                                          Margin="0,0,4,0"
                                          VerticalAlignment="Center"/>
                                
                                <!-- 图层图标 -->
                                <TextBlock Grid.Column="1"
                                           Text="{Binding Icon}"
                                           FontSize="14"
                                           VerticalAlignment="Center"
                                           Margin="0,0,6,0"/>
                                
                                <!-- 图层名称 -->
                                <TextBlock Grid.Column="2"
                                           Text="{Binding Name}"
                                           FontSize="12"
                                           Foreground="#2C3E50"
                                           VerticalAlignment="Center"/>
                                
                                <!-- 颜色指示器 -->
                                <Border Grid.Column="3"
                                        Width="12" Height="12"
                                        CornerRadius="2"
                                        Margin="8,0"
                                        VerticalAlignment="Center"
                                        IsVisible="{Binding !IsGroup}">
                                    <Border.Background>
                                        <SolidColorBrush Color="{Binding SymbolColor}"/>
                                    </Border.Background>
                                </Border>
                                
                                <!-- 透明度滑块 -->
                                <Slider Grid.Column="4"
                                        Classes="opacity-slider"
                                        Value="{Binding Opacity, Mode=TwoWay}"
                                        IsVisible="{Binding !IsGroup}"
                                        VerticalAlignment="Center"
                                        ToolTip.Tip="透明度"/>
                                
                                <!-- 上下移动按钮 -->
                                <StackPanel Grid.Column="5" Orientation="Horizontal" Spacing="2" Margin="4,0,0,0">
                                    <Button Content="⬆️"
                                            FontSize="10"
                                            Padding="4,2"
                                            Background="Transparent"
                                            BorderThickness="0"
                                            ToolTip.Tip="上移图层"
                                            Command="{Binding $parent[UserControl].((vm:MappingLayerViewModel)DataContext).MoveLayerUpCommand}"
                                            CommandParameter="{Binding}"/>
                                    <Button Content="⬇️"
                                            FontSize="10"
                                            Padding="4,2"
                                            Background="Transparent"
                                            BorderThickness="0"
                                            ToolTip.Tip="下移图层"
                                            Command="{Binding $parent[UserControl].((vm:MappingLayerViewModel)DataContext).MoveLayerDownCommand}"
                                            CommandParameter="{Binding}"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </TreeDataTemplate>
                </TreeView.ItemTemplate>
            </TreeView>
        </DockPanel>
        
        <!-- 添加图层弹出面板 -->
        <Border IsVisible="{Binding ShowAddLayerPanel}"
                Background="#CC000000"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch">
            <Border Background="White"
                    CornerRadius="8"
                    Margin="10"
                    Padding="12"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    MinWidth="220"
                    MaxHeight="350">
                <DockPanel>
                    <!-- 标题 -->
                    <Border DockPanel.Dock="Top" Margin="0,0,0,12">
                        <TextBlock Text="添加图层" FontSize="14" FontWeight="Bold" Foreground="#2C3E50"/>
                    </Border>
                    
                    <!-- 按钮 -->
                    <StackPanel DockPanel.Dock="Bottom" 
                                Orientation="Horizontal" 
                                HorizontalAlignment="Right" 
                                Spacing="8" 
                                Margin="0,12,0,0">
                        <Button Content="取消" 
                                Command="{Binding CancelAddLayerCommand}" 
                                Padding="16,6"/>
                        <Button Content="确定" 
                                Command="{Binding ConfirmAddLayerCommand}" 
                                Padding="16,6"
                                Background="#3498DB"
                                Foreground="White"/>
                    </StackPanel>
                    
                    <!-- 资源列表 -->
                    <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
                        <ItemsControl ItemsSource="{Binding AvailableResources}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Padding="4,3" CornerRadius="4" Margin="0,1"
                                            Background="{Binding IsChecked, Converter={x:Static vm:BoolToBackgroundConverter.Instance}}">
                                        <Grid ColumnDefinitions="Auto,Auto,*">
                                            <CheckBox Grid.Column="0" 
                                                      IsChecked="{Binding IsChecked, Mode=TwoWay}" 
                                                      VerticalAlignment="Center"/>
                                            <TextBlock Grid.Column="1" 
                                                       Text="{Binding Icon}" 
                                                       FontSize="14" 
                                                       Margin="4,0,8,0"
                                                       VerticalAlignment="Center"/>
                                            <StackPanel Grid.Column="2" VerticalAlignment="Center">
                                                <TextBlock Text="{Binding Name}" 
                                                           FontSize="11" 
                                                           Foreground="#2C3E50"/>
                                                <TextBlock Text="{Binding Category}" 
                                                           FontSize="9" 
                                                           Foreground="#7F8C8D"/>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </DockPanel>
            </Border>
        </Border>
    </Grid>
    </Border>
</UserControl>
